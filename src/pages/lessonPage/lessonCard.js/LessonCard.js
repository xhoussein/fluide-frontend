import React from "react";
import ReusableCard from "../../../components/card/Card";

const LeassonCard = (props) => {
  return (
    <div>
      <ReusableCard {...props} />
    </div>
  );
};

export default LeassonCard;
