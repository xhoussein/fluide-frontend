import { Typography } from "@mui/material";
import React from "react";

const TermsAndCondition = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Typography variant="h2">Terms And Condition </Typography>
    </div>
  );
};

export default TermsAndCondition;
